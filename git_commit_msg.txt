Add favorite and suggestion datasets for note items

- Created `fav.ts` with a dataset of 10 favorite items including titles and image URLs.
- Created `suggest.ts` with a dataset of 30 suggestion items, expanding on the favorites with additional titles and image URLs.