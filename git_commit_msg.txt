feat: Add error handling for data fetching in note page

- Implemented try-catch block to handle potential connection errors during data fetching.
- Ensured that empty arrays are returned if data retrieval fails, allowing the component to manage empty states gracefully.